{# src/Mcms/UserBundle/Resources/views/Patient/changePassword.html.twig #}
{% extends '::patientDashboard.html.twig' %}

{% block title %}{{ app.user }} - Zmiana hasła użytkownika{% endblock %}

{% block content %}
	<h1>Zmiana hasła użytkownika <em>{{app.user}}</em></h1>
    <form action="{{ path('patient.updatePassword')}}" method="post" {{ form_enctype(form) }}>
        {{form_errors(form)}}
        {{ form_widget(form) }}
        <input type="submit" />
    </form>
{% endblock %}