{# app/Resources/formErrors.html.twig #}

{% block field_errors %}
	{% if errors is defined and errors %}
		<div class="message msgerror">
			<a class="close"></a>
    		{% if errors|length > 0 %}
    			<ul class="error_list">
        			{% for error in errors %}
            			<li>{{ error.messageTemplate|trans(error.messageParameters, 'validators') }}</li>
        			{% endfor %}
    			</ul>
    		{% endif %}
		</div>
	{% endif %}
{% endblock field_errors %}